# Buttons Bebe production Caddy entrypoint (redacted source layout).
#
# This file is intentionally import-only. Each imported fragment owns one
# public site so a console change cannot silently delete warehouse or exchange
# routing. Keep credentials and deployment-only paths out of Git; see README.md.

import sites/support.caddy
import sites/exchange.caddy
import sites/warehouse.caddy
